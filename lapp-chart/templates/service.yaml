apiVersion: v1
kind: Service
metadata:
  name: {{ include "lapp-chart.fullname" . }}
  labels:
    {{- include "lapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lapp-chart.selectorLabels" . | nindent 4 }}
